# Environment Configuration
# ========================
# .env        — non-sensitive defaults (committed)
# .env.local  — API keys and secrets (gitignored, never commit)
#
# Setup: copy this file to .env.local and fill in your API keys.

# API Keys (put in .env.local)
GEMINI_API_KEY=AIzaSyXxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxx
# ANTHROPIC_API_KEY is NOT used — Claude Code authenticates via Max subscription (ADR-0011)
