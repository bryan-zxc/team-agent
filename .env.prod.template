# Production environment configuration
# Copy to .env.prod and fill in real values:
#   cp .env.prod.template .env.prod

# ---------- Caddy ----------
SITE_ADDRESS=team-agent.example.ts.net

# ---------- Frontend ----------
NEXT_PUBLIC_API_URL=https://team-agent.example.ts.net/api

# ---------- PostgreSQL ----------
POSTGRES_USER=teamagent
POSTGRES_PASSWORD=<strong-password-here>
POSTGRES_DB=teamagent
DATABASE_URL=postgresql+asyncpg://teamagent:<strong-password-here>@postgres:5432/teamagent

# ---------- Redis ----------
REDIS_URL=redis://redis:6379

# ---------- AI Service — API keys ----------
GEMINI_API_KEY=
OPENAI_API_KEY=
# ANTHROPIC_API_KEY is NOT used — Claude Code authenticates via Max subscription (ADR-0011)

# ---------- AI Service — model configuration ----------
GEMINI_MODEL=gemini-3-pro-preview
MODEL=claude-opus-4-6
OPENAI_MODEL=gpt-5.2

# ---------- Auth ----------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://team-agent.example.ts.net/api/auth/callback
FRONTEND_URL=https://team-agent.example.ts.net

# ---------- General ----------
TEAM_AGENT_ENV=prod
